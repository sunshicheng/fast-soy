# 前端项目快速参考

## 📁 目录结构速查

### 核心目录

| 目录 | 作用 | 重要文件 |
|------|------|---------|
| `src/main.ts` | 应用入口 | 初始化所有插件和配置 |
| `src/App.vue` | 根组件 | 全局配置和布局 |
| `src/views/` | 页面组件 | 所有业务页面 |
| `src/router/` | 路由配置 | 路由定义和守卫 |
| `src/store/` | 状态管理 | Pinia store 模块 |
| `src/service/` | API 服务 | HTTP 请求封装 |
| `src/components/` | 公共组件 | 可复用组件 |
| `src/hooks/` | 组合式函数 | 通用 hooks |
| `src/locales/` | 国际化 | 多语言文本 |

---

## 🔑 关键文件说明

### 入口文件

```
src/main.ts              # 应用入口，初始化所有模块
src/App.vue              # 根组件
index.html               # HTML 模板
```

### 路由相关

```
src/router/
├── index.ts             # 路由实例创建
├── elegant/
│   ├── routes.ts        # 自动生成的路由配置（不要手动修改）
│   ├── imports.ts       # 自动生成的路由导入（不要手动修改）
│   └── transform.ts     # 路由转换工具
├── guard/               # 路由守卫
│   ├── route.ts         # 路由权限守卫
│   └── title.ts         # 页面标题守卫
└── routes/
    └── index.ts         # 自定义路由配置（customRoutes）
```

### 状态管理

```
src/store/modules/
├── app/                 # 应用配置（语言、主题等）
├── auth/                # 认证信息（用户、token、角色）
├── route/               # 路由和菜单
├── tab/                 # 标签页管理
└── theme/               # 主题配置
```

### API 服务

```
src/service/
├── api/                 # API 接口定义
│   ├── auth.ts         # 认证 API
│   ├── route.ts         # 路由 API
│   └── system-manage.ts # 系统管理 API
└── request/             # HTTP 请求封装
    └── index.ts         # Axios 实例
```

### 页面组件

```
src/views/
├── _builtin/            # 内置页面（登录、404、500）
├── home/                # 首页
├── manage/              # 系统管理
│   ├── user/            # 用户管理
│   ├── role/            # 角色管理
│   └── menu/            # 菜单管理
└── ...
```

---

## 🛠️ 常用命令

```bash
# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 生成路由（重要！）
pnpm gen-route

# 代码检查
pnpm lint

# 类型检查
pnpm typecheck

# Git 提交（规范提交）
pnpm commit
```

---

## 📝 开发流程

### 添加新页面

1. **创建页面文件** → `src/views/your-page/index.vue`
2. **运行路由生成** → `pnpm gen-route`
3. **添加国际化** → `src/locales/langs/zh-cn.ts`
4. **配置路由** → `src/router/routes/index.ts` (静态路由)
5. **添加 API** → `src/service/api/your-api.ts` (如需要)
6. **添加类型** → `src/typings/api.d.ts` (如需要)

### 添加 API

1. **创建 API 文件** → `src/service/api/your-api.ts`
2. **导出 API** → `src/service/api/index.ts`
3. **添加类型** → `src/typings/api.d.ts`
4. **在页面中使用** → `import { fetchYourApi } from '@/service/api'`

---

## 🎯 常用代码片段

### 表格页面模板

```vue
<script setup lang="tsx">
import { useTable } from '@/hooks/common/table';
import { fetchGetList } from '@/service/api';

const {
  columns,
  data,
  loading,
  getData,
  pagination,
  searchParams
} = useTable({
  apiFn: fetchGetList,
  columns: () => [/* 列配置 */],
  apiParams: { current: 1, size: 10 }
});
</script>

<template>
  <NCard>
    <NDataTable
      :columns="columns"
      :data="data"
      :loading="loading"
      :pagination="pagination"
    />
  </NCard>
</template>
```

### API 调用

```typescript
import { fetchGetList } from '@/service/api';

const { data, error } = await fetchGetList({
  current: 1,
  size: 10
});
```

### Store 使用

```typescript
import { useAuthStore } from '@/store/modules/auth';

const authStore = useAuthStore();
const userInfo = authStore.userInfo;
```

### 路由跳转

```typescript
import { useRouterPush } from '@/hooks/common/router';

const { routerPush } = useRouterPush();
routerPush({ name: 'your_route' });
```

### 国际化

```typescript
import { $t } from '@/locales';

$t('common.add')  // "新增"
$t('route.home')  // "首页"
```

---

## 📚 相关文档

- [项目结构说明](./项目结构说明.md) - 详细的项目结构说明
- [添加页面指南](./添加页面指南.md) - 添加页面的完整步骤

---

**文档版本**: 1.0  
**最后更新**: 2025-11-06

